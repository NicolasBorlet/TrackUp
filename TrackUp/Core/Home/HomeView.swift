import SwiftUI

struct HomeView: View {
    var body: some View {
        VStack {
            Text("Home View")
        }
        .frame(maxWidth: .infinity, maxHeight: .infinity)
        .withGradientBackground()
    }
}
