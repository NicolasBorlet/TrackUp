import SwiftUI

struct CalendarView: View {
    var body: some View {
        VStack {
            Text("Calendrier")
        }
        .frame(maxWidth: .infinity, maxHeight: .infinity)
        .withGradientBackground()
    }
}
