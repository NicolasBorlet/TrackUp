import SwiftUI

struct StatisticsView: View {
    var body: some View {
        VStack {
            Text("Statistiques")
        }
        .frame(maxWidth: .infinity, maxHeight: .infinity)
        .withGradientBackground()
    }
}
