import SwiftUI

struct ProfileView: View {
    var body: some View {
        VStack {
            Text("Profil")
        }
        .frame(maxWidth: .infinity, maxHeight: .infinity)
        .withGradientBackground()
    }
}
